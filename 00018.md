<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>添加微信</title>
<style>
  body { font-family: "Microsoft YaHei", Arial, sans-serif; text-align: center; margin: 0;
         background: linear-gradient(160deg, #0b1e4d, #0f2f6b); color: #fff; min-height: 100vh; }
  .box { padding: 60px 20px; }
  h2 { font-size: 30px; margin-bottom: 8px; letter-spacing: 2px; }
  .sub { color: #9db4e0; font-size: 15px; margin-bottom: 40px; }
  .wechat { font-size: 44px; font-weight: bold; color: #7dff00; letter-spacing: 3px;
            background: rgba(255,255,255,.08); display: inline-block; padding: 18px 40px;
            border-radius: 12px; border: 1px solid rgba(125,255,0,.4); margin-bottom: 36px; }
  .btn { display: block; width: 80%; max-width: 360px; margin: 12px auto; padding: 16px 0;
         border: none; border-radius: 12px; font-size: 20px; font-weight: bold; cursor: pointer; }
  .btn-copy { background: #7dff00; color: #0b1e4d; }
  .btn-open { background: #1f8a2f; color: #fff; }
  .tip { color: #9db4e0; font-size: 14px; margin-top: 32px; line-height: 1.8; }
  .ok { color: #7dff00; font-weight: bold; display: none; }
</style>
</head>
<body>
<div class="box">
  <h2>加 我 微 信</h2>
  <div class="sub">微信扫码不方便? 复制号码直接搜索添加</div>

  <div class="wechat" id="wid">4724197</div>

  <button class="btn btn-copy" onclick="copyWx()">📋 点这里复制微信号</button>
  <div class="ok" id="ok">✅ 已复制! 去微信粘贴搜索即可</div>

  <button class="btn btn-open" onclick="openWx()">打开微信（手机端尝试）</button>

  <div class="tip">
    复制号码后:<br>
    打开微信 → 右上角 ➕ → 添加朋友 → 粘贴搜索 → 添加
  </div>
</div>

<script>
function copyWx() {
  var txt = document.getElementById('wid').textContent.trim();
  var done = function () {
    document.getElementById('ok').style.display = 'block';
    setTimeout(function () { document.getElementById('ok').style.display = 'none'; }, 4000);
  };
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(txt).then(done, function () { fallback(txt); done(); });
  } else {
    fallback(txt); done();
  }
}
function fallback(txt) {
  var ta = document.createElement('textarea');
  ta.value = txt; ta.style.position = 'fixed'; ta.style.opacity = '0';
  document.body.appendChild(ta); ta.select();
  try { document.execCommand('copy'); } catch (e) {}
  document.body.removeChild(ta);
}
function openWx() {
  location.href = 'weixin://dl/chat?username=4724197';
}
</script>
</body>
</html>
